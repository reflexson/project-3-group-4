{
  "name": "project-3-group-4",
  "version": "0.1.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "concurrently \"cd server && server.js\"  \"cd client && npm start\"",
    "develop": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build",
    "start:prod": "cross-env NODE_ENV=production cd server && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "concurrently": "^5.1.0",
    "cross-env": "^7.0.3"
  }
}
